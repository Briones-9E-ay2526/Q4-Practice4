function openSesame(){

    let carCounter = 10;
    const maxCars = 30;

    while (carCounter < maxCars){
        window.alert("Car Number " + carCounter + " is entering");
        carCounter += 1;
    }

    window.alert(Garageisfull)
}

